apiVersion: v1
kind: Service
metadata:
  name: {{ include "team-based-demo.fullname" . }}
  labels:
    {{- include "team-based-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team-based-demo.selectorLabels" . | nindent 4 }}
